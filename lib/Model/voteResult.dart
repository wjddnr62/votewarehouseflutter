class VoteResult {
  String date; // ex ) 2020-03-21
  String question; // ex ) q1
  String title; // ex ) title
  String email;
  int idx;
  String answer; // ex ) 1

  VoteResult({this.date, this.question, this.title, this.email, this.idx, this.answer});
}