class User {
  String email;
  String pass;
  String gender;
  String age;
  String address;
  String phone;
  String token;
  bool addInfomation;
  bool push;

  User({this.email, this.pass, this.gender, this.age, this.address, this.phone, this.token, this.addInfomation, this.push});
}